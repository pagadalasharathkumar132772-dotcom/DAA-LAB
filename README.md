  PRACTICAL-1
  Summary:
  In this practical, we implemented Bubble Sort, Selection Sort, Insertion Sort, Merge Sort, and Quick Sort.
  We compared there  execution times and observes that merge sort and quick sort are faster then other sorting algorthim,especially for large dataset.
  Conclusion:
  From this practical we learned different sorting algorthim have different performance levels.Bubble sort,Selection sort and Insertion sort are simple and suitable for       small data sets.Merge sort and Quick sort are suitable for large datasets.

  
  PRACTICAL-2
  Summary:
  In this practical,we implemented Linear search and Binary search and compare there execution times.We observe that Linear search has time complexity of O(n) because it      checks each element one by one,while binary search has a time complexity of O(log n) and searches faster by repeatedly dividing the sorted array into halves.
  Conclusion:
  Through this practical,we understood that Binary search faster than Linear search for large sorted data.Linear search is simple and useful for small or unsorted             data,while  Binary search is more efficient for large sorted data.


  PRACTICAL-3
  Summary:
  In this practical,we implemented the Max-Heap Sort algorithm and observed how it arranges elements in ascending order.The algorthim first builds a max-heap and then         repeatedly places the largest element in its correct position.Its time complexity isO(n log n).
  Conclusion:
  Through this practical,we learned how Max-Heap sort works and how it efficiently sorts data.Since its time complexity is O(n log n),it performs well even for large          datasets.

  
 PRACTICAL-4
 Summary:
 In this practical,we implemented the factorial program using iterative and recursive methods and compared their execution.Both methods give the same result,but they work    differently.The time complexity of both method is O(n).
 Conclusion:
 Through this practical,we lerned how to calculate factorial using both methods.The iterative method uses a loop,while the recursive method uses function calls.Both have     O(n) time complexity,but the iterative method generally uses less memory.
